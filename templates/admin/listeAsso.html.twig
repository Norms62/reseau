{% extends 'base.html.twig' %}

{% block title %}Liste Asso{% endblock %}

{% block body %}
<center>
    <h1  style="color : #F4B82A ; font-weight : bold">Liste des associations</h1><br><br>
</center>

<table class="table table-striped table-bordered">

    <thead>
        <tr>
            <th>Nom Association </th>
            <th>Région - Pays</th>
            <th> Nombre total de tickets </th>
        </tr>
    </thead>
        {% for l in listeAsso %}
            <tr>
                <td> <a href= "http://127.0.0.1:8000/listeAsso/{{l.nom_asso}}" style="color: #0000FF"> {{ l.nom_asso }} </a></td>
                {% if l.region_RE =='' %}
                    <td> {{l.region_adm}}</td>
                {% else %}
                    <td> {{l.region_RE}} - {{l.region_adm}}</td>
                {% endif %}
                <td> <a href= "http://127.0.0.1:8000/listeTicketParAsso/{{l.nom_asso}}" style="color: #0000FF"> {{nbTicket[l.id]}}
                
            </tr>
        {% endfor %}
    <tbody>

    </tbody>
</table>
    
{% endblock %}