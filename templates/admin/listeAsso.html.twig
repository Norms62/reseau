{% extends 'base.html.twig' %}

{% block title %}Liste Asso{% endblock %}

{% block body %}
<br>
 <form>
  <input type="button" class="btn btn-info " value="Retour" onclick="history.go(-1)"> 
</form>
<br>

<center>
    <h1  style="color : #F4B82A ; font-weight : bold">Liste des associations  </h1><br><br>
</center><br>

<button type="button" class="btn btn-info" onclick="document.location.href='{{path('ajout_asso')}}';" >Ajouter un association</button><br><br>

<table class="table table-striped table-bordered">

    <thead>
        <tr>
            <th>Nom Association </th>
            <th>Région - Pays</th>
            <th> Nombre total de tickets </th>
            <th> Modifier </th>
        </tr>
    </thead>
        {% for l in listeAsso %}
            <tr>
                <td> <a href= "https://ticketting.reseau-entreprendre.org/index.php/listeAsso/{{l.nom_asso}}" style="color: #0000FF"> {{ l.nom_asso }} </a></td>
                {% if l.region_RE =='' %}
                    <td> {{l.region_adm}}</td>
                {% else %}
                    <td> {{l.region_RE}} - {{l.region_adm}}</td>
                {% endif %}
                <td> <a href= "https://ticketting.reseau-entreprendre.org/index.php/listeTicketParAsso/{{l.nom_asso}}" style="color: #0000FF"> {{nbTicket[l.id]}} </td>
                <td> <a href= "https://ticketting.reseau-entreprendre.org/index.php/modifAsso/{{l.nom_asso}}" style="color: #0000FF"> modifier </td>
                
            </tr>
        {% endfor %}
    <tbody>

    </tbody>
</table>
    
{% endblock %}